# Local Windows development - uncomment the line below if needed
# Docker containers use Java from the base image (this file is excluded via .dockerignore)
org.gradle.java.home=C:\\jdk-17
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true
