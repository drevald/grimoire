<beans:beans 
	xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans	
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd 
	http://www.springframework.org/schema/security	
	http://www.springframework.org/schema/security/spring-security-3.1.xsd">

    <!-- <http auto-config="true"  use-expressions="true">
        <intercept-url pattern="/login.jsp" access="permitAll" />
        <intercept-url pattern="/logout" access="permitAll" />
        <intercept-url pattern="/accessdenied" access="permitAll" />
        <intercept-url pattern="/**" access="hasRole('ROLE_USER')" />
        <form-login login-page="/login.jsp" default-target-url="/dict" authentication-failure-url="/login.jsp?error=true" />
        <logout logout-success-url="/logout" />
    </http> -->

    <http access-denied-page="/error403.jsp">
        <intercept-url pattern="*" access="ROLE_ADMIN" />
        <intercept-url pattern="/dict*" access="ROLE_USER"/>
        <intercept-url pattern="/dict/words*" access="ROLE_ANONYMOUS"/>
        <intercept-url pattern="/dict/view*" access="ROLE_ANONYMOUS"/>
        <form-login login-page="/login.jsp" default-target-url="/dict"
            authentication-failure-url="/login.jsp?error=true" />
        <logout logout-url="/logout" logout-success-url="/login.jsp" />
        <anonymous username="guest" granted-authority="ROLE_ANONYMOUS" />
        <remember-me />
    </http>

<!--    <authentication-manager>-->
<!--        <authentication-provider>-->
<!--            <jdbc-user-service data-source-ref="dataSource"-->
<!--            users-by-username-query="SELECT accountname, password, 'true' FROM account WHERE accountname = ?"-->
<!--            authorities-by-username-query="SELECT accountname, 'ROLE_USER' FROM account WHERE accountname = ?"/>-->
<!--        </authentication-provider>-->
<!--    </authentication-manager>-->

    <authentication-manager alias="authenticationManager">
		<authentication-provider>
			<jdbc-user-service data-source-ref="dataSource"
                users-by-username-query="SELECT accountname, password, enabled FROM account WHERE accountname=?;"
                authorities-by-username-query="SELECT accountname, 'ROLE_USER' FROM account WHERE accountname = ?"
            />
		</authentication-provider>
	</authentication-manager>

</beans:beans>